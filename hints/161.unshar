#!/bin/sh

# 161 creates a patch file you are expected to apply. So extract it and reverse apply.
cd $TEMP
sed -n -e '/^sed.*patchfile/,/^SHAR_EOF/p' < $1 | sh -x
cd $ROOT211
patch -R -p1 < $TEMP/patchfile
exit 0
